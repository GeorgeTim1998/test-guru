<h1><%= @answer.question.body %></h1>

<h2><%= @answer.body %></h2>

<p><%= @answer.correct %></p>

<table>
  <tr>
    <td><%= link_to "Edit", edit_answer_path(@answer) %></td>
    <td>
      <%= link_to "Del",
                  @answer,
                  method: :delete,
                  data: { confirm: "You sure?" } %>
    </td>
    <td><%= link_to "Back", question_path(@answer.question_id) %></td>
  </tr>
</table>
